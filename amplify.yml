version: 1
applications:
  - appRoot: apps/vue-sdk
    frontend:
      buildPath: apps/vue-sdk
      phases:
        preBuild:
          commands:
            - nvm use 18
            - yarn install
        build:
          commands:
            - yarn build:prod
      artifacts:
        baseDirectory: apps/vue-sdk/dist
        files:
          - "**/*"
      cache:
        paths:
          - "node_modules/**/*"
