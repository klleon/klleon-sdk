version: 1
applications:
  - appRoot: apps/react-sdk
    frontend:
      phases:
        preBuild:
          commands:
            - nvm use 18
            - yarn install
        build:
          commands:
            - cd apps/react-sdk
            - yarn build:prod
      artifacts:
        baseDirectory: apps/react-sdk/dist
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
      buildPath: /
